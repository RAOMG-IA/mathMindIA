# Presentation / HTTP

Rutas, controllers y middlewares REST. Consumen los DTOs de `packages/shared-types` y los Casos de Uso de `src/application/use-cases`.

Pendiente de implementar — requiere definir primero los contratos DTO/API (ver punto pendiente en STATUS.md).
