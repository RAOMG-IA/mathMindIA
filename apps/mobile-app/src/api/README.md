# API

Clientes TanStack Query que consumen `backend-api`, tipados con los DTOs de `packages/shared-types`.

Pendiente de implementar — requiere que los contratos DTO/API estén definidos primero (ver pendiente en [STATUS.md](../../../../docs/STATUS.md)).
