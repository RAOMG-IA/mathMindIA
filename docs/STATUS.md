# Estado del Proyecto

> Documento vivo — se actualiza libremente según avanza el trabajo. No es un ADR: no registra decisiones estables, solo progreso. Para decisiones, ver [ADR-000](ADR-000_Estructura.md) y siguientes.

## Pendientes Prioritarios

### 1. ~~DOMAIN.md~~ ✅

Completado como [ADR-004_domain.md](ADR-004_domain.md).

### 2. ~~ADR-005 Adaptive Difficulty Engine~~ ✅

Completado como [ADR-005](ADR/ADR-005-adaptive-difficulty-engine.md).

### 3. ~~math-topics.md~~ ✅

Completado como [ADR-006_math_topics.md](ADR/ADR-006_math_topics.md).

### 4. ~~User Stories~~ ✅

Completadas en [docs/user-stories/](user-stories/). US-003 renombrada a "Iniciar Sesión de Entrenamiento" para no confundirse con US-002 Login. US-007 quedó sin caso de uso asignado — pendiente añadir UC-007 al definir Casos de Uso (#5).

### 5. ~~Casos de Uso~~ ✅

Completados en [docs/use-cases/](use-cases/). UC-001 original se dividió en UC-001 (Generate Exercise, Batch/IA) y UC-008 (Select Next Exercise, tiempo real/determinista). Se añadió UC-007 (Get User Statistics) para cerrar el hueco de US-007.

### 6. ~~Línea Base de Seguridad~~ ✅

Completada como [ADR-012_linea_base_seguridad.md](ADR/ADR-012_linea_base_seguridad.md), referenciada desde `.ai/AGENTS.md`. Sustituye a un paquete inicial de 5 ADR (`docs/ADR/Security/`, eliminado) que modelaba una postura de seguridad de plataforma en producción (Vault, RBAC formal, Zero Trust) desproporcionada para el estado actual del proyecto y centrada en proteger a los agentes de desarrollo entre sí en vez del producto. ADR-012 cubre en su lugar lo aplicable ahora: gestión de secretos, prompt injection acotado a UC-001/UC-003, protección de datos de menores (relevante por `AcademicLevel.Primaria`), y regla mínima de contraseñas.

## Estado Actual

| Área | Estado |
|---|---|
| Visión Producto | ✅ 100% |
| Arquitectura | ✅ 95% |
| Monorepo | ✅ 95% (scaffolding creado, `npm install` y `turbo typecheck` verificados en verde — 13/13 tareas) |
| Sistema Multiagente | ✅ 95% |
| Documentación | ✅ 90% |
| Dominio | ✅ 100% diseño + contratos de repositorio materializados como TS real en `packages/shared-domain` (VOs, entidades, 6 repositorios) |
| Casos de Uso | ✅ 100% (UC-001 a UC-011 — UC-009/UC-010 y UC-011 añadidos, huecos detectados al construir el backend real y al definir US-008 respectivamente) |
| Seguridad | ✅ 100% línea base (ADR-012) **implementada**: hash bcrypt real, JWT real, mensajes de login genéricos, verificación de autorización (IDOR) en los 3 Casos de Uso que operan sobre una `Session` existente. RBAC/Vault/Zero Trust diferidos deliberadamente, ver "Fuera de alcance" del ADR |
| Contratos IA | ✅ 100% forma de datos (DTOs API + contrato Qwen) materializada como TS real, con validación en tiempo de ejecución (Zod, ADR-001 adenda 2026-08-06) ya implementada en `QwenClient` |
| Modelo de Datos Físico | ✅ 100% [ADR-013](ADR/ADR-013_modelo_datos_fisico.md) (+ adenda: `UserCredentials`, `Session.topic`/`ratingAtStart`, `Exercise.timeLimitMs`) + [ADR-014](ADR/ADR-014_rag.md) (`RagIngestionRecord`, `RagChunk` + pgvector) + `database/schema.prisma` real, **sincronizado con Postgres al completo** (`prisma db push`, extensión `vector` instalada por el usuario). Migración formal `prisma migrate dev` sigue pendiente de un bug de collation del Postgres local, ajeno al proyecto |
| Infraestructura (Database/API/LLM) | ✅ **Backend real de punta a punta, con persistencia real, incluido RAG**: 6 `Prisma*Repository` (TDD, 23/23 tests de integración) + **RAG (UC-011) verificado end-to-end de verdad**: `IngestKnowledgeBaseUseCase` (TDD, 5/5) + 4 adaptadores reales, los 4 en Green (`NodeIngestionFileSystem` 4/4, `PrismaRagIngestionRepository` 2/2, `PostgresKnowledgeBaseIndex` 2/2 contra pgvector real). Prueba manual real: fichero `.txt` depositado → `npm run ingest:rag` → embedding real (Xenova/all-MiniLM-L6-v2) → fila en `rag_chunks`/`rag_ingestion_records` → `search()` recupera el chunk por similitud semántica real, sin fakes. `main.ts` conectado al adaptador real (ya no hay no-op). **LLM real**: `QwenClient` + `QwenHintGenerator` (TDD, 5/5 y 2/2), `LangChainChatModel`/`XenovaEmbedder` siguen sin tests automáticos (gap aceptado, dependen de red/descarga de modelo — la descarga en sí ya se verificó manualmente). `LangChainChatModel` renombrado desde `LangChainQwenModel` (2026-08-08): agnóstico de proveedor, verificado en la práctica con DeepSeek además de Qwen/DashScope — las variables de entorno (`AI_API_KEY`/`AI_BASE_URL`/`AI_MODEL_NAME`) ya no llevan "QWEN" en el nombre. **Auth real**: `BcryptPasswordHasher`/`JwtTokenIssuer`. |
| Implementación | ✅ 95% — `AdaptiveDifficultyEngine` (8/8) + 11 Casos de Uso + 5 Controllers + middleware de auth + 2 adaptadores de auth + 6 `Prisma*Repository` + `IngestKnowledgeBaseUseCase` + 4 adaptadores RAG, todo TDD Red→Green y **verificado end-to-end con datos reales**. 79 tests verdes en `backend-api` + 15 en `ai-engine` + **31/31 tests de integración reales** (0 bloqueados). Backend arrancable, probado manualmente de punta a punta (incluida persistencia entre reinicios y retrieval semántico real). Falta: migración formal (bloqueada por Postgres local), catálogo de Temas como tabla (fuera de alcance), Redis. |
| Git / Repositorio remoto | ✅ Inicializado, commit inicial en `main`, push a `https://github.com/RAOMG-IA/mathMindIA.git` verificado. |
| `mobile-app` (frontend) | 🟡 Diseño 100% [ADR-015](ADR/ADR-015_mobile_app_screens.md). `src/store`/`src/api` completos (26/26 tests). `NeuralLoader` (UI, sin test) + 2 pantallas reales: `LoginScreen` (US-002), `RegisterScreen` (US-001) — `QueryClientProvider`/`SafeAreaProvider` ya montados en `app/_layout.tsx`. Pendiente: Home, Ejercicio, Resumen, Estadísticas — `app/index.tsx` sigue siendo el placeholder |

## Próximo Paso Recomendado

1. ~~DOMAIN.md~~ ✅
2. ~~ADR-005 Adaptive Difficulty Engine~~ ✅
3. ~~math-topics.md~~ ✅
4. ~~User Stories~~ ✅
5. ~~Casos de Uso~~ ✅
6. ~~Scaffolding de `apps/`+`packages/`~~ ✅ (npm workspaces + Turborepo, solo tooling/estructura — ver `.ai/prompts/architecture.md`)
7. ~~`npm install` + `turbo typecheck`~~ ✅ (13/13 tareas correctas tras corregir `langchain` a `^1.5.0` — la versión `^0.3.0` inicial chocaba con `@langchain/core` 1.x)

10. ~~Decidir librería de navegación para `mobile-app`~~ ✅ Expo Router — registrado en [ADR-001](ADR-001_LenguajesMetodologias.md), `apps/mobile-app/app/` con enrutado por archivos.
11. ~~Línea Base de Seguridad~~ ✅ [ADR-012](ADR/ADR-012_linea_base_seguridad.md), sustituye al paquete `docs/ADR/Security/` eliminado.
12. ~~Contratos de repositorio~~ ✅ `packages/shared-domain/src/{value-objects,entities,repositories}` — TypeScript real (sin lógica), verificado con `turbo typecheck`/`lint`. `email` añadido a `User` (hueco detectado al tipar `findByEmail`).
13. ~~Contratos de frontera (DTOs API + contrato Qwen)~~ ✅ `packages/shared-types/src/dtos` (7 DTOs) y `apps/ai-engine/src/prompts` (GenerateExercise/GenerateHint). `ExercisePublicDto` excluye `correctAnswer`/`explanation`/`difficulty` deliberadamente. Hueco detectado: `hintsUsed` necesita contador efímero (Redis) antes de que exista `Answer` — no resuelto, documentado en `Hint.ts`. Validación en tiempo de ejecución del contrato Qwen sigue pendiente (librería sin decidir).
14. ~~Modelo de Datos Físico~~ ✅ [ADR-013](ADR/ADR-013_modelo_datos_fisico.md) — hueco detectado por el usuario al revisar US-001 (no había esquema ni ORM ratificado). Prisma confirmado. `database/schema.prisma` verificado con `prisma validate`/`generate` reales.
15. ~~Tests de `AdaptiveDifficultyEngine`~~ ✅ (TDD Red) `packages/shared-domain/src/services/AdaptiveDifficultyEngine.test.ts` — 8 casos, valores calculados a mano contra ADR-005, trazados a US-004. Hueco real detectado y documentado en ADR-005: el K provisional (cold start) necesita un contador de intentos por nivel que no existe en `User` — diferido.
16. ~~Implementación de `computeNextDifficulty`~~ ✅ (TDD Green) Primer ciclo TDD completo del proyecto. `vitest run` → 8/8 en verde a la primera. Hueco preexistente detectado y corregido de paso: `turbo run test` fallaba en toda regla para paquetes sin tests (`vitest run` sale con código 1 sin tests) — se añadió `--passWithNoTests` a los 8 `package.json` con script `test`.
17. ~~Revisión de `.ai/skills/*.md`~~ ✅ 5 hallazgos, todos resueltos: rutas `ARCHITECTURE.md` mal escritas, referencia a ADR-012 añadida a las 11 skills, solape architecture/documentation aclarado, ejemplos de `knowledge-manager.md` sincronizados con el código real. (El punto de `reviewer.md` asumiendo flujo PR quedó resuelto de facto al inicializar git — ver #18.)
18. ~~Git inicializado + push a GitHub~~ ✅ `git init`, remote `origin` → `https://github.com/RAOMG-IA/mathMindIA.git`, commit inicial (158 archivos, verificado sin secretos), `push -u origin main` confirmado en el remoto.
19. ~~Esqueletos de Infrastructure (Database/API/LLM)~~ ✅ (`declare class`, sin lógica) `Prisma*Repository` (5), `*Controller` (5), `QwenClient`. Impedimento real señalado antes de generarlos: faltan Casos de Uso implementados y sus Tests — la implementación real de estas clases sigue bloqueada hasta entonces.
20. ~~Primer Caso de Uso completo: UC-002 + UC-004~~ ✅ (TDD Red→Green) `UpdateDifficultyUseCase` (UC-004, wrapper de orquestación sobre `computeNextDifficulty`) y `ValidateAnswerUseCase` (UC-002, invoca UC-004), elegidos por el usuario vía AskUserQuestion sobre UC-005/UC-008. 8/8 tests verdes con `InMemory{Session,Exercise,Answer,User}Repository` + `FixedClock`/`SequentialIdGenerator` (`packages/shared-testing/src/mocks`, antes vacío). Huecos de dominio detectados y resueltos al tipar: `Exercise.timer: Timer` (materialización de un hueco ya asumido en ADR-004), `INITIAL_RATING` (Difficulty por defecto), puertos `IdGenerator`/`Clock` nuevos en `packages/shared-domain/src/ports`.
21. ~~UC-007 Get User Statistics~~ ✅ (TDD Red→Green) `GetUserStatisticsUseCase`, elegido tras descartar UC-003 (bloqueado por el hueco ya documentado de `hintsUsed`/contador efímero) y UC-006 (necesitaría decidir cómo trackear la variación de `userRating` desde el inicio de sesión, hueco de diseño no resuelto). Agregación por Tema (ADR-006) con `MIN_ATTEMPTS_PER_TOPIC=3`/`TOP_N=3` como constantes documentadas (el propio UC delega esos umbrales "a definir al implementar"). 3/3 tests verdes, sin dependencia de `SessionRepository` (el contrato `AnswerRepository.findByUserId` ya resuelve el join como responsabilidad de infraestructura).
22. ~~UC-003 Generate Hint + UC-006 End Session~~ ✅ (TDD Red→Green) Los dos huecos de diseño pendientes de #21 se resolvieron por indicación explícita del usuario ("avanza con UC3 y UC6"): `Session.ratingAtStart: Difficulty` (snapshot al crear la sesión, mismo patrón que `Exercise.timer` — cierra el hueco de "variación de rating" de UC-006) y el puerto `HintUsageTracker` en `packages/shared-domain/src/ports` (contador efímero sesión+ejercicio, cierra el hueco de `hintsUsed`/UC-003, ya anticipado en la nota de diseño de `packages/shared-types/src/dtos/Hint.ts`). `GenerateHintUseCase` depende de un puerto local `HintGenerator` (no de `apps/ai-engine` directamente — son apps/cajas separadas por ARCHITECTURE.md); la implementación real (adaptador a `QwenClient`) sigue diferida, mismo patrón que Prisma/controllers. 10/10 tests verdes. Nuevos dobles: `InMemoryHintRepository`, `InMemoryHintUsageTracker`.
23. ~~UC-005 Start Session + UC-008 Select Next Exercise~~ ✅ (TDD Red→Green) Último hueco de diseño pendiente resuelto: entidad `Tema` + `TemaRepository` (catálogo de referencia de ADR-006, sin `save` — se puebla por seed/migración, no por un Caso de Uso). `SelectNextExerciseUseCase` consulta banda ±150 (ADR-005) y amplía a ±300 solo si viene vacía; selecciona el candidato de `difficulty` más cercano al `userRating` (judgment call documentado, el UC no fija algoritmo). `StartSessionUseCase` compone la implementación real de `SelectNextExerciseUseCase`. 11/11 tests verdes. Con esto, **los 6 Casos de Uso de `application/use-cases/README.md` quedan completos**.

24. ~~QwenClient real + adaptador HintGenerator~~ ✅ (TDD Red→Green) Primera pieza de Infrastructure con implementación real, no esqueleto. Decisiones confirmadas por el usuario vía AskUserQuestion: transporte backend-api↔ai-engine por **import directo in-process** (no HTTP — ARCHITECTURE.md nunca especifica transporte y no hay evidencia de despliegue independiente en este TFM), **Zod** para validación de forma del output de Qwen (registrado como adenda en ADR-001, mismo patrón que Expo Router), alcance **QwenClient + adaptador** (no solo el cliente aislado). Puerto nuevo `ChatModel` (`apps/ai-engine/src/llm`) desacopla `QwenClient` de LangChain concreto — permite TDD sin red real (5/5 tests). `LangChainQwenModel` (implementación real de `ChatModel`, envuelve `ChatOpenAI`) queda sin tests automáticos, gap aceptado explícitamente. Hueco detectado al construir el adaptador: el puerto `HintGenerator` no llevaba `previousHints` pese a que UC-003 documenta "pistas progresivas" (US-005) — corregido en `GenerateHintUseCase` (recopila pistas previas vía `HintRepository` antes de generar), 1 test nuevo. `QwenHintGenerator` (adaptador real, `apps/backend-api/src/infrastructure/ai`) 2/2 tests con fake estructural de `QwenClient`. UC-001 (Generate Exercise Batch) sigue sin Caso de Uso — `QwenClient.generateExercise` ya está listo y testeado para cuando se aborde.

Con el primer ciclo Red→Green completo, el patrón queda establecido para el resto de lógica de negocio (casos de uso, resto de servicios de dominio): User Story + AC → Tests (Test Agent) → Implementación (Developer Agent), cada fase con confirmación explícita del usuario antes de avanzar a la siguiente. Los Casos de Uso de Application (#20-23) confirman que el patrón también funciona a nivel de orquestación (no solo lógica de dominio pura), y que los huecos de diseño detectados y diferidos pueden resolverse en una iteración posterior sin tener que replantear lo ya construido. #24 confirma que el mismo patrón TDD funciona también para Infrastructure real (no solo Application/Domain), con la particularidad de que el adaptador que habla con la red real (`LangChainQwenModel`) queda deliberadamente fuera de la cobertura automática.

25. ~~API REST (mapeo de rutas)~~ ✅ `ARCHITECTURE.md` ("API REST (Rutas)") — una ruta por DTO existente, mapeada a los 5 Controllers ya declarados. El usuario pidió inicialmente una User Story para esto; se señaló el desajuste con `docs/user-stories/README.md` (las User Stories no incluyen diseño técnico) y se resolvió como tarea del Architecture Agent en su lugar. 2 huecos de contrato corregidos (`RequestHintRequestDto.elapsedMs`, `SessionController.startSession(userId, ...)`) y 1 hueco de autorización (IDOR: `EndSessionUseCase`/`ValidateAnswerUseCase`/`GenerateHintUseCase` no verifican dueño de la `Session`) documentado como requisito explícito para cuando se implementen los Controllers reales.
26. ~~UC-001 Generate Exercise (Batch)~~ ✅ (TDD Red→Green) `GenerateExerciseBatchUseCase` (`apps/ai-engine/src/batch/`) — cierra el último Caso de Uso pendiente. Usa `QwenClient.generateExercise` (ya listo), reintenta hasta `MAX_ATTEMPTS=3` (judgment call documentado, el UC no fija N) si el resultado viola las invariantes de `Exercise` (ADR-004: `type='Test'` ⇒ exactamente 3 opciones y `correctAnswer` incluida), `difficulty` inicial = punto medio del `difficultyRange` del Tema para ese nivel (ADR-006), `timer.limitMs` fijo en 15s (placeholder documentado — ningún ADR/UC fija el límite de tiempo de un ejercicio generado por IA). Paso 1 del UC (elegir qué Tema/nivel tiene escasez) queda explícitamente fuera — llega resuelto como input, decidido por un scheduler todavía sin construir (mismo criterio que UC-008 flujo 2b). 5/5 tests verdes. `apps/ai-engine/src/batch/README.md` corregido (referenciaba UC-003 incorrectamente, esa generación vive en `backend-api`).

**Con esto, los 7 Casos de Uso originales de STATUS.md están completos** (UC-001 a UC-008, con la división ya conocida UC-001/UC-008 y el UC-007 añadido para US-007).

27. ~~Backend real: Controllers + rutas Express + auth~~ ✅ (TDD Red→Green) El usuario pidió "comenzamos con el backend" — construir los Controllers reales + Express siguiendo el mapa de #25. Cadena de huecos detectados y resueltos, cada uno surgido al construir el siguiente consumidor real (mismo patrón de toda la sesión):
    - **UC-009 Register / UC-010 Login**: US-001/US-002 nunca tuvieron Caso de Uso asignado (mismo motivo que UC-007) — cerrado antes de poder implementar `AuthController`.
    - **Puertos nuevos**: `PasswordHasher`/`TokenIssuer` (`packages/shared-domain/src/ports`) + entidad `UserCredentials`/`UserCredentialsRepository` (credenciales deliberadamente fuera del agregado `User`, decisión ya fijada en ADR-004).
    - **`SEED_RATING_BY_LEVEL` corregido**: `INITIAL_RATING` (constante plana en 1200) ignoraba que ADR-005 fija semillas *distintas* por nivel (Primaria 800, Secundaria 1200, Bachillerato 1600, Ingeniería 2000) — bug real, no solo un hueco; sembrar el rating correcto al registrarse lo sacó a la luz. Los 4 consumidores (`ValidateAnswerUseCase`, `EndSessionUseCase`, `SelectNextExerciseUseCase`, `StartSessionUseCase`) actualizados; ningún test existente cambió de resultado (todos usaban `Secundaria`, cuya semilla coincide con el valor plano anterior).
    - **Hueco IDOR corregido**: `EndSessionUseCase`/`ValidateAnswerUseCase`/`GenerateHintUseCase` ganan `userId` en su input y verifican `session.userId === input.userId`, señalado en #25 y cerrado aquí con 3 tests nuevos.
    - **`Session.topic` añadido**: al construir `AnswerController` (compone UC-008 tras cada respuesta, "a nivel de contrato HTTP" según el propio DTO) se detectó que `Session` no guardaba `topic` — sin eso, UC-008 no podía seguir sirviendo ejercicios del mismo tema tras el primer ejercicio. `StartSessionUseCase` actualizado para persistirlo.
    - **`HintUsageTracker.get` añadido**: `AnswerController` necesita leer `hintsUsed` sin incrementar (para no confiar en el cliente) — el puerto solo tenía `incrementAndGet`.
    - **`GetUserStatisticsUseCase` ampliado**: `GetUserStatisticsResponseDto` exige `area` (por tema) y `academicLevel` (nivel actual, no el mapa completo de ratings) que el Caso de Uso no producía — se añadió `TemaRepository` como dependencia nueva para resolver `area`.
    - **5 Controllers reales**: `AuthController`, `SessionController`, `AnswerController` (compone `ValidateAnswerUseCase`+`SelectNextExerciseUseCase`, omite `nextExercise` sin fallar toda la respuesta si el pool está agotado — flujo 2b), `HintController`, `StatisticsController`.
    - **`BcryptPasswordHasher`/`JwtTokenIssuer`**: a diferencia de `LangChainQwenModel`/Prisma, son cómputo puro (sin red) — **sí** tienen cobertura de test completa (bcrypt real, jsonwebtoken real, incluyendo tokens expirados/con secreto distinto).
    - **`authMiddleware`**: `extractBearerToken` (función pura) + `createAuthMiddleware`, testeados con objetos Request/Response mínimos, sin servidor real.
    - **`routes.ts`/`main.ts`**: wiring puro (sin tests automáticos, mismo criterio que Prisma/LangChain) — verificado **manualmente arrancando el servidor** (`npx tsx src/presentation/main.ts`) y probando con `curl` el flujo completo: registro → login → iniciar sesión → responder (Elo real aplicado, rating cambió de 800.00 a 805.36) → finalizar sesión → estadísticas → 401 sin token.
    - `main.ts` usa repositorios en memoria (`packages/shared-testing`) porque `Prisma*Repository` sigue en esqueleto — el servidor arranca y funciona de verdad, pero sin persistencia entre reinicios; documentado explícitamente en el propio archivo.

    83 tests verdes (73 `backend-api` + 10 `ai-engine`), `npx turbo run typecheck lint test` → 31/31 tareas en verde.

28. ~~Hueco de proceso: Reviewer y Security nunca invocados~~ ✅ El usuario detectó que, al implementar Register/Login, no se había invocado al Security Agent pese a que `.ai/AGENTS.md` fija un flujo obligatorio (Product→Architecture→Test→Developer→**Reviewer→Security**→Documentation, "Ninguna fase puede omitirse"). Verificación confirmó que el hueco era más amplio: **Reviewer Agent no se había usado ni una vez en todo el proyecto** (su fichero de trazabilidad tenía además un bug de copia/pega, cabecera "# Test Agent" en vez de "# Reviewer Agent"), Security solo se había usado una vez (revisión de ADR-012/skills, no desde entonces), y Documentation nunca había registrado una entrada propia (aunque la documentación sí se mantuvo al día dentro de otras entradas). Pasadas retroactivas reales sobre el código de autenticación (la superficie de mayor riesgo, y la que motivó la pregunta):
    - **Reviewer**: 4 hallazgos de deuda técnica menor (adaptadores `IdGenerator`/`Clock` inline en vez de clases nombradas, `handleError` siempre HTTP 400 sin distinguir tipos de error, promesa top-level sin `await` en el seed de `main.ts`, `catch` genérico en `AnswerController.tryComposeNextExercise`) — documentados, no bloqueantes, no corregidos en esta pasada (Reviewer no modifica código).
    - **Security**: 3 hallazgos, los 3 corregidos con TDD — (1) **fuga de información confirmada**: `routes.ts` reenviaba el mensaje interno de las Use Cases tal cual, permitiendo distinguir "sesión inexistente" de "sesión de otro usuario" en las rutas protegidas por la verificación IDOR (`/sessions/end`, `/answers`, `/hints`) — corregido con `errorMapping.ts` (función pura testeada, mensaje genérico + 403 para esas tres rutas). (2) **sin política de contraseña**: `RegisterUseCase` aceptaba cualquier longitud pese a que US-001/ADR-012 la dejaban "a definir al implementar" — corregido con mínimo de 8 caracteres (OWASP ASVS L1). (3) **hardening, no vulnerabilidad activa**: `JwtTokenIssuer.verify` no fijaba `algorithms: ['HS256']` explícitamente — confirmado que jsonwebtoken ya rechazaba `alg=none` por defecto (el ataque clásico de confusión de algoritmo no aplica, este sistema nunca usa claves asimétricas), pero se fijó igualmente como defensa en profundidad.
    - **Documentation**: reconocimiento de que el contenido documental de la sesión sí está al día, solo faltaba el registro formal — sin retrabajo de documentación ya escrita.

    6 tests nuevos, 79 tests verdes en `backend-api` (antes 73). `npx turbo run typecheck lint test` → 31/31 en verde.

29. ~~Hueco de proceso (continuación de #28): Director/Orchestrator/Knowledge Manager sin gestión real~~ ✅ El usuario identificó que el hueco de #28 era un síntoma de algo más amplio: `.ai/prompts/{director,orchestrator,knowledge-manager}.md` eran solo su cabecera — ninguna tarea de la sesión pasó realmente por la jerarquía de decisión (Usuario→Director→Orchestrator→Knowledge Manager→Agentes Operativos) que `ADR-002`/`AGENTS.md` ya definen; todo fue directo de la petición del usuario a Architecture/Test/Developer. Propuso la causa raíz correcta y la solución: un **contrato de handoff estructurado y obligatorio** (`objective`, `scope`, `constraints`, `references`, `acceptance`, `risks`, `required_agents`, + `requester` añadido) que Orchestrator (o Director) debe validar completo antes de que cualquier tarea empiece a ejecutarse — exactamente el punto de control mecánico que habría detectado el hueco de #28 antes de que ocurriera, no después. Adoptado como adenda de `ADR-002` (no ADR nuevo — refina la misma decisión, mismo criterio que la adenda de Zod en ADR-001), con `.ai/AGENTS.md`, las 3 skills afectadas y un primer handoff real registrado en `.ai/prompts/orchestrator.md` (el de definir el propio contrato — dogfooding). Se adopta hacia adelante; no se reescribe el histórico de tareas ya cerradas.

30. ~~US-008 Consolidar Base de Conocimiento (RAG)~~ ✅ El usuario pidió una historia de usuario para `ai-engine` que permita subir ficheros (bancos de problemas, notas, pistas) para consolidar RAG. Primera tarea despachada con el contrato de handoff de #29 ya en uso real (`.ai/prompts/orchestrator.md`, segundo handoff). Redefinida a continuación por el propio usuario: no hay subida vía aplicación — `ai-engine` no depende de ningún `User`; el flujo es un directorio local configurado donde se depositan ficheros, que se mueven a un directorio de histórico tras procesarse, registrando fecha y estado. Al ser acceso directo al sistema de ficheros del servidor, el actor es el administrador del sistema (infraestructura, no un rol de aplicación) — resuelve de raíz el hueco de rol que la primera versión dejaba pendiente. Sigue sin Caso de Uso asignado (mismo patrón que US-007), y sigue pendiente para Security Agent la tercera vía de contenido no confiable hacia el prompt de Qwen, no cubierta todavía por [ADR-012](ADR/ADR-012_linea_base_seguridad.md) (que solo contemplaba UC-001/UC-003).

31. ~~Prisma*Repository real (Database)~~ ✅ (TDD Red→Green) El usuario eligió este paso tras el informe de avance de la sesión. Cierra el último bloque de Infrastructure que quedaba en esqueleto. Cadena de huecos detectados antes de poder implementar, mismo patrón de toda la sesión:
    - **Schema desactualizado**: `Session.topic`/`ratingAtStart` y `Exercise.timeLimitMs` existían en el dominio (huecos ya resueltos en #20-#27) pero nunca llegaron a `database/schema.prisma` — se habría perdido esos campos en cada guardado. Corregido (adenda ADR-013).
    - **6º repositorio, no 5**: `UserCredentialsRepository` faltaba en el alcance original — el propio `schema.prisma` dejaba dicho "se añadirá cuando se implemente US-001/US-002", ya implementados (#27). Sin él, un `User` sobreviviría a un reinicio y sus credenciales no, rompiendo el login.
    - **Driver adapter de Prisma 7**: `@prisma/adapter-pg` añadido — el cliente generado usa el compilador de queries WASM, sin motor nativo embebido, y ya no acepta pasar `DATABASE_URL` directo al constructor.
    - **Decisiones confirmadas con el usuario**: sin Docker (entorno mono-servidor, conexión vía el `DATABASE_URL` real de `.env`); tests de integración reales contra ese Postgres (no gap aceptado como `LangChainQwenModel`), en un proyecto Vitest separado y fuera del `test` por defecto.
    - **Bloqueo real de infraestructura, no de código**: `prisma migrate dev` falla en este entorno por una discordancia de versión de collation en la base patrón `template1` de Postgres (típico tras una actualización de Windows), no relacionado con el schema. Resuelto pragmáticamente con `prisma db push` (sincroniza igual las tablas); la migración formal queda pendiente de que se arregle ese Postgres local.
    - **Judgment call**: `Answer` (dominio) no lleva `userId` pero la columna desnormalizada de `answers` sí lo necesita (ADR-013) — resuelto con una lectura extra de `Session` dentro de `PrismaAnswerRepository.save()`, sin tocar el contrato del puerto.
    - **Hueco detectado al conectar el seed real**: el `Exercise` de arranque de `main.ts` usaba un id aleatorio en cada arranque — con persistencia real habría creado un duplicado por cada reinicio. Corregido con un id fijo. El `void exercises.save(...)` que Reviewer ya había señalado (#28) pasa a `await` — con memoria era solo estilo, con una escritura real a red es una condición de carrera real.

    23/23 tests de integración verdes contra Postgres real (`npm run test:integration`), 79/79 tests por defecto sin cambios (DB-free por diseño), `npx turbo run typecheck lint test` → 31/31 en verde. **Verificación manual decisiva**: servidor reiniciado a mitad de la prueba end-to-end — mismo usuario, mismo rating (804.29), misma estadística de tema tras el reinicio. Es la prueba concreta de persistencia real que ningún repositorio en memoria pudo dar en toda la sesión.

32. ~~UC-011 Ingest Knowledge Base (define el Caso de Uso de US-008)~~ ✅ El usuario pidió definir el Caso de Uso de US-008: indexado + embeddings con LangChain en `ai-engine`, conectados a UC-001/UC-003. Trajo la mayoría de decisiones ya resueltas (script disparado manualmente o por cron, directorio de histórico, registro de ingesta en Postgres); quedaba abierta la tecnología de indexado vectorial. Propuso Chroma; se señaló que el cliente JS de LangChain para Chroma exige un servidor HTTP aparte (sin modo embebido en Node, a diferencia de Python) — el usuario preguntó por pgvector como alternativa y, tras comparar ambas explícitamente, se confirmó vía AskUserQuestion: **pgvector**, no Chroma (cero servicios nuevos, consistencia transaccional con el registro de ingesta, un solo backup — mismo criterio "sin servicios nuevos en mono-servidor" ya aplicado a Postgres). Embeddings: modelo local (`@xenova/transformers`), sin API externa, confirmado también vía AskUserQuestion.

    Alcance de esta tarea: solo diseño/documentación (Architecture Agent, restricción "no implementar código productivo"). Decisiones clave: sin tagging fichero↔Tema en la ingesta — la asociación ocurre por similitud semántica en el momento de generar (query construida desde `Tema`/`Exercise`, no desde metadata del fichero); formatos v1 limitados a texto plano/Markdown; dos modelos nuevos en `schema.prisma` (`RagIngestionRecord`, `RagChunk` con columna `Unsupported("vector(384)")`, extensión `pgvector` habilitada declarativamente); nuevos puertos `RagIngestionRepository`/`KnowledgeBaseIndex` en `packages/shared-domain` (sin implementación); UC-001 y UC-003 amendados (no un UC nuevo) con un paso de retrieval antes de construir el prompt — si no hay material consolidado, generan igual que hoy, sin caso especial. Riesgo de seguridad (tercera vía de contenido no confiable hacia el prompt, ya señalado en US-008/ADR-012) repetido explícitamente para Security Agent, no mitigado aquí.

    `npx prisma validate` en verde tras añadir los modelos — ningún fichero de código tocado, ninguna dependencia instalada.

33. ~~UC-011 implementado (TDD Red→Green)~~ ✅ El usuario pidió implementar lo diseñado en #32 nada más commitearlo. Cadena de huecos detectados durante la implementación, mismo patrón de toda la sesión:
    - **`@langchain/community` descartado**: arrastraba `@browserbasehq/stagehand` como peer dependency (conflicto de `dotenv`) para una integración de vectorstore que no se iba a usar (el adaptador de pgvector es SQL crudo propio, no la abstracción de LangChain) — se instaló solo `@langchain/textsplitters` (ai-engine) y `@xenova/transformers` (backend-api).
    - **`RagChunk.ingestionRecordId` (FK obligatoria) vs. `KnowledgeBaseIndex.index()` (puerto genérico, sin conocer el registro de ingesta)**: incompatibilidad detectada al implementar `PostgresKnowledgeBaseIndex`. Corregida quitando la FK y añadiendo `sourceFileName`/`chunkIndex` como campos planos en `RagChunkInput` — misma trazabilidad práctica sin acoplar el puerto a su único llamador actual.
    - **`main.ts` NO se conectó a `PostgresKnowledgeBaseIndex` real**: con la tabla `rag_chunks` inexistente (pgvector bloqueado), el adaptador real habría roto la generación de pistas con una excepción SQL en cada petición, en vez de degradar con normalidad como exige el AC "Tema sin material consolidado sigue funcionando" de US-008. Se usa un objeto no-op inline (mismo criterio que `idGenerator`/`clock`, ya inline en ese archivo) — cambiar a la implementación real es un cambio de una línea, documentado en el propio archivo, en cuanto pgvector esté disponible.
    - **Bloqueo transitivo descubierto**: el plan asumía que solo `PostgresKnowledgeBaseIndex` (con columna vectorial) quedaría bloqueado por pgvector. Al intentar `prisma db push` se confirmó que bloquea **todo el schema**, incluyendo `PrismaRagIngestionRepository` (sin ninguna columna vectorial) — porque Prisma aplica el schema completo de una vez, no tabla a tabla. Ambos adaptadores quedan implementados y en Red confirmado por la razón correcta, Green pendiente de que el usuario instale la extensión.

    5/5 tests de `IngestKnowledgeBaseUseCase` verdes (fakes nuevos: `FakeEmbedder`, `InMemoryKnowledgeBaseIndex`, `InMemoryRagIngestionRepository`, `InMemoryIngestionFileSystem`). `NodeIngestionFileSystem` 4/4 verde (real, sin BBDD). En el mismo mensaje en que se detectó el bloqueo, el usuario instaló pgvector — cerrado a continuación sin pausa:
    - `prisma db push` → verde. Los 2 tests antes bloqueados pasan a ejecutarse, y uno de ellos (`PostgresKnowledgeBaseIndex`) reveló un bug real (no del bloqueo): `FakeEmbedder` generaba vectores de 8 dimensiones, la columna es `vector(384)` — pgvector lo rechaza con un error claro. Corregido (`FakeEmbedder` a 384 dimensiones, coherente con `XenovaEmbedder`/el schema). El propio test de similitud también se rediseñó: comparar contra un fake sin semántica real no puede garantizar que "fracciones" quede más cerca de "fracciones" que de "Paris" — se corrigió para verificar la mecánica de orden por distancia (SQL), no la calidad semántica del fake, que no es su trabajo demostrar.
    - `main.ts` conectado a `PostgresKnowledgeBaseIndex`/`XenovaEmbedder` reales, ya sin el no-op inline.
    - **Verificación manual end-to-end con datos reales**: un `.txt` depositado, `npm run ingest:rag` (con `RAG_INPUT_DIR`/`RAG_HISTORY_DIR` de prueba), descarga real del modelo `Xenova/all-MiniLM-L6-v2` la primera vez, fichero movido al histórico, fila en `rag_ingestion_records` (`Processed`, 1 chunk) y en `rag_chunks` con su embedding real. `search()` con una query semánticamente relacionada recuperó el chunk correcto — la primera prueba de todo el proyecto de retrieval semántico real, sin fakes en ningún punto de la cadena.

    `npx turbo run typecheck lint test` → **31/31 en verde**. `npm run test:integration` → **31/31 en verde** (0 bloqueados).

**Siguiente paso lógico tras #33**: un único pendiente de infraestructura, no bloqueante — migración formal (`prisma migrate dev`), bloqueada por un bug de collation del Postgres local, ajeno al proyecto (se sigue sincronizando con `prisma db push` mientras tanto). Con `#31` y `#33`, el backend tiene su columna vertebral técnica completa de punta a punta (dominio → aplicación → infraestructura real → presentación, incluido RAG) **verificada con datos reales, no solo con TDD** — es el primer punto de la sesión en que absolutamente ninguna pieza de Infrastructure queda como gap aceptado o bloqueada por el entorno. De cara a la memoria del TFM, #28-#29 son también un buen ejemplo documentado de que el propio sistema multiagente puede fallar en seguir su flujo (y de cómo se detecta y corrige con un mecanismo concreto, no solo con buenas intenciones) — probablemente material citable en la memoria sobre gobernanza de sistemas multiagente.

34. ~~ADR-015 Arquitectura de pantallas de `mobile-app`~~ ✅ El usuario pidió planificar Casos de Uso/User Stories para `mobile-app`; inicialmente eligió User Stories/Casos de Uso nuevos por pantalla y rectificó ("planificamos desde un adr como me recomendabas") hacia el enfoque de Architecture recomendado — completar una decisión que el propio scaffolding ya anticipaba (`src/screens/README.md`: "una pantalla por User Story relevante"), no inventar producto nuevo. Resuelve los tres huecos reales que quedaban: mapeo ruta de Expo Router ↔ User Story (con route groups `(auth)`/`(app)` y guard de autenticación centralizado), persistencia del `sessionToken` en el dispositivo, y el patrón de `src/api/` (bloqueado hasta ahora por falta de contrato — ya resuelto por `apps/backend-api/openapi.yaml`, #33 anterior). Zustand/TanStack Query no son decisión nueva (ya ratificados en ADR-001) — el ADR solo formaliza su reparto. US-004/US-005/US-006(acción) comparten una única pantalla (`session/[sessionId]`), reflejando que las tres ocurren "en una sesión de entrenamiento activa" según las propias historias, no una simplificación arbitraria.

    **Revisión posterior del propio usuario, misma tarea**: (1) el despliegue es **Android + iOS + Web** (Expo/`react-native-web`, una sola base de código) — invalidaba la decisión inicial de persistencia del token: `expo-secure-store` no existe en navegador (no hay Keychain/Keystore). Corregido con una abstracción `TokenStorage` seleccionada por `Platform.OS` — `expo-secure-store` en nativo, `localStorage` en web, con el riesgo de seguridad más débil de esta última (sin cifrado de SO, expuesta a XSS) documentado explícitamente como no mitigado en v1, señalado para Security Agent. (2) el modo/nivel debe poder verse desde una cabecera/configuración global, no solo al arrancar una sesión — resuelto con un header común a las 5 pantallas autenticadas (`(app)/_layout.tsx`) mostrando email + nivel/rating + accesos directos a Estadísticas y Home (donde vive el único formulario real de cambio, US-003). Hueco real detectado al diseñarlo: `LoginResponseDto`/`RegisterResponseDto` (`openapi.yaml`) solo devuelven `userId`/`sessionToken`, nunca el email — se cachea en Zustand en el momento del login/registro en vez de re-consultarlo; el nivel/rating sí existe en `GET /users/me/statistics` (UC-007) y el header reutiliza el mismo hook de TanStack Query que la pantalla de Estadísticas, sin endpoint nuevo.

    Alcance: solo diseño/documentación (Architecture Agent). Ningún componente React Native ni test escrito — ciclo TDD posterior (Test Agent → Developer Agent), mismo patrón que UC-011 en #32/#33.

35. ~~Primera implementación real de `mobile-app`: `TokenStorage` + `useSessionStore` + `fetchClient`~~ ✅ (TDD Red→Green) El usuario dio luz verde ("no, comenzamos") a implementar lo diseñado en #34. Primer código real de `mobile-app` de toda la sesión — hasta ahora solo scaffolding y documentación. Se empezó por la pieza más fundamental (de la que dependen `src/api` y las pantallas): persistencia de sesión.
    - **`TokenStorage.ts`**: puerto local (no de `shared-domain` — es un concepto propio del cliente, no del dominio matemático). Sus implementaciones concretas (`expo-secure-store`/`localStorage`) quedan sin test automático, mismo criterio que `LangChainChatModel`/`XenovaEmbedder`.
    - **`useSessionStore.ts`** (Zustand): `hydrate`/`login`/`logout` reciben `TokenStorage` inyectado, testeado con un fake local `InMemoryTokenStorage`. Persiste `{userId, email, sessionToken}` como un único blob — no solo el token — porque el email nunca vuelve del backend tras el login (`LoginResponseDto`/`RegisterResponseDto` solo traen `userId`/`sessionToken`), y solo en memoria se habría perdido al reiniciar la app.
    - **`fetchClient.ts`**: `EXPO_PUBLIC_API_BASE_URL` (nuevo `.env.example`), cabecera `Authorization: Bearer` salvo en `/auth/register`/`/auth/login`, error lanzado con el campo real `error` del body (`errorMapping.ts`, no `message`).
    - **Hueco de tipos corregido**: `tsc --noEmit` fallaba en el test de `fetchClient` porque TypeScript infería `[]` para los argumentos de un mock de `fetch` sin firma explícita — corregido tipando los parámetros del mock.

    9/9 tests verdes (`useSessionStore.test.ts` 5/5, `fetchClient.test.ts` 4/4). `npx turbo run typecheck lint test` → **31/31 en verde** en todo el monorepo — primera vez que `mobile-app` aporta tests reales al pipeline (antes solo `--passWithNoTests`). Pendiente: implementaciones concretas de `TokenStorage`, hooks de TanStack Query de `src/api`, y las propias pantallas.

36. ~~Implementaciones reales de `TokenStorage`~~ ✅ El usuario pidió cerrar el hueco que #35 dejaba explícito: `expo-secure-store`/`localStorage` seguían sin implementar, solo el puerto y el fake de test. `expo-secure-store` instalado (`^57.0.1`, alineado con `expo@^57.0.0`) — única dependencia nueva que ADR-015 dejaba pendiente. `SecureStoreTokenStorage` (nativo, Keychain/Keystore) y `WebTokenStorage` (`localStorage`, riesgo ya documentado en ADR-015) implementan el puerto; `createTokenStorage()` selecciona una u otra por `Platform.OS`. Las tres quedan sin test automático — dependen de un módulo nativo, del DOM o de la resolución de `react-native`/Metro (`.ios`/`.android`/`.web`), ninguno disponible bajo Vitest/node — mismo criterio ya aceptado para `LangChainChatModel`/`XenovaEmbedder`/`main.ts`. `npx turbo run typecheck lint test` → 31/31 en verde, sin tests nuevos (nada de lo añadido era testeable bajo este runner).

37. ~~Clases/hooks/abstracciones de TanStack Query (`src/api`)~~ ✅ (TDD Red→Green) El usuario pidió generar las piezas de TanStack Query para las 7 rutas de negocio de `openapi.yaml` (`/health` omitida deliberadamente — sin consumidor en ninguna pantalla). Diseño: separar la lógica pura y testeable (`requests/*.ts`, funciones `*Request(dto)` que envuelven `fetchClient`) de los hooks en sí (`hooks/use*.ts`, `useMutation`/`useQuery`) — estos últimos no son testeables sin `QueryClientProvider`+`renderHook` (tooling de React Testing Library, ausente en todo el monorepo), mismo criterio de wiring puro ya aplicado a `routes.ts`/`main.ts` en `backend-api`.
    - **7/7 tests verdes** en `requests/{auth,session,answer,hint,statistics}.test.ts` — verifican método, ruta y body serializado por cada función, sin duplicar la cobertura de `fetchClient.test.ts`.
    - **`useRegister`/`useLogin`** escriben la sesión en `useSessionStore` desde `onSuccess`, tomando el `email` de la propia petición (`variables`) — el backend no lo devuelve tras el login, mismo hueco ya resuelto en #35.
    - **`queryKeys.ts`**: `useUserStatistics` usa una `queryKey` compartida para que el header global de `(app)` y `(app)/statistics` (ambos previstos en ADR-015) reutilicen la misma caché en vez de duplicar la petición.

    `npx turbo run typecheck lint test` → 31/31 en verde (16/16 tests de `mobile-app`, antes 9). Con esto, `src/api` queda completo salvo la composición real en pantallas (siguiente paso lógico junto con `src/screens`).

38. ~~Invalidación de caché al cambiar de nivel/modo (`invalidateQueries`)~~ ✅ (TDD Red→Green) El usuario pidió que la caché de TanStack Query se limpie cuando el usuario cambia de nivel académico o de modo de juego. Único punto real donde eso ocurre: `StartSessionRequestDto` (US-003, `useStartSession`) — no hay ninguna otra mutación que toque `mode`/`academicLevel`. `useStartSession` invalida `queryKeys.statistics` en `onSuccess`, sin comparar contra el valor anterior (judgment call: arrancar sesión ya es el único disparador, y volver a pedir el dato es más simple y más seguro que llevar la cuenta de si realmente cambió).
    - **Hueco de testabilidad resuelto, no aceptado como gap**: a diferencia del resto de `hooks/` (sin test, requieren `QueryClientProvider`+`renderHook`), la propia invalidación sí se pudo extraer y testear — `QueryClient` es una clase plana de `@tanstack/query-core`, instanciable y espiable (`vi.spyOn`) sin renderizar ningún componente React. `invalidateStatisticsOnSessionStart(queryClient)` queda como función standalone, testeada aparte del hook que la invoca.

    1/1 test nuevo (`useSession.test.ts`), `npx turbo run typecheck lint test` → 31/31 en verde (17/17 tests de `mobile-app`, antes 16).

39. ~~`NeuralLoader`: primer componente de UI real de `mobile-app`~~ ✅ El usuario pidió un loader animado con estética fMRI (perfil craneal + cerebro con zonas de actividad + símbolos matemáticos emergentes), aportando un prompt de diseño exacto. Prototipado primero como HTML/CSS/SVG (Artifact) para validar visualmente antes de invertir en el port a React Native — dos iteraciones: la primera con geometría de cráneo/cerebro aproximada a mano ("no tiene las formas correctas"), corregida rediseñando el cráneo como un único contorno anatómico y el cerebro como tres masas separadas (corteza/temporal/cerebelo) en vez de un contorno con muescas; la segunda integrando el SVG real de cráneo/cerebro que aportó el usuario (Inkscape, coordenadas relativas) — bounding box calculado con un script (no a ojo, las curvas relativas no son triviales de acotar a mano) para recortar el `viewBox` correctamente. Aprobado el diseño, se pidió implementar el componente.
    - **Dependencias nuevas** (judgment call): `react-native-svg` (trazado + gradientes), `react-native-reanimated` (animaciones en hilo nativo, requiere `react-native-reanimated/plugin` en `babel.config.js`), `expo-blur` (blur del status pill).
    - **Hueco real detectado al verificar**: `react-native-web`/`react-dom` no estaban instalados — `npx expo export --platform web` fallaba directamente. Sin ellos, el target Web que fija ADR-015 (Android+iOS+Web) no funcionaba en absoluto; no es solo una dependencia de esta tarea, cierra un hueco de la propia decisión de despliegue.
    - **Adaptaciones deliberadas donde RN no tiene equivalente CSS**: `filter:blur()` de las zonas de actividad → `RadialGradient` de SVG; `backdrop-filter:blur()` del status pill → `expo-blur` (nativo) con fallback a fondo semitransparente en web; parpadeo del cursor (`step-end`) → fundido rápido ida-vuelta; blur de los símbolos emergentes, sin equivalente en texto de RN, compensado con el arco de opacidad ya marcado.
    - **Arquitectura de animación**: `react-native-reanimated` (`useSharedValue`/`useAnimatedStyle`/`useAnimatedProps`/`interpolate`) sustituye los `@keyframes` de CSS — cada keyframe de varias paradas (p. ej. el pulso de zona 0.92→1→1.02→0.95) se modela como un `progress` animado 0→3 e `interpolate` en vez de un salto discreto entre etapas (hueco corregido durante la implementación: la primera versión usaba `Math.floor` sobre el progreso, perdiendo la interpolación suave).
    - **Verificación real, no solo typecheck**: montado temporalmente en `app/index.tsx`, `npx expo export --platform web` → bundle de 1147 módulos sin errores de import/runtime, revertido después. No hay forma de probar iOS/Android sin un simulador real — gap aceptado explícitamente, mismo criterio que otras piezas de infraestructura de la sesión sin acceso a hardware real.

    Sin test automático — es UI puramente visual/de animación (igual que las implementaciones concretas de `TokenStorage`). `npx turbo run typecheck lint test` → 31/31 en verde, sin tests nuevos.

40. ~~`LoginScreen` (US-002): primera pantalla real de `mobile-app`~~ ✅ (TDD Red→Green) El usuario pidió definir la pantalla de login usando el `NeuralLoader` como base visual, con validación de email y la normativa de contraseña de Security — "si no está definido, añádelo a shared-utils".
    - **Hueco real confirmado**: la política de contraseña (mínimo 8 caracteres, OWASP ASVS L1, hallazgo Security 2026-08-07) solo existía como constante local de `RegisterUseCase` — no reutilizable. Extraída a `@mathmind/shared-utils` (`isValidEmail`, `isValidPassword`/`MIN_PASSWORD_LENGTH`, TDD, 9/9 tests), única fuente de verdad; `RegisterUseCase` refactorizado para usarla (79/79 tests de `backend-api` sin cambios de resultado).
    - **Conflicto real de resolución de módulos, detectado por el smoke-test de bundle web (no por `tsc`)**: `shared-utils` es el primer paquete `shared-*` con exports de *valor* (no solo interfaces) consumido en tiempo de ejecución por `mobile-app` — un barrel con `export * from './x.js'` satisface a NodeNext (`backend-api`, exige extensión) pero rompe a Metro (`mobile-app`, no traduce `.js`→`.ts`). `shared-types` nunca lo sufrió porque sus exports son interfaces puras, borradas al compilar. Resuelto consolidando `shared-utils` en un único fichero sin imports relativos internos.
    - **`validateLoginForm`** (`src/screens/LoginScreen.validation.ts`, TDD, 4/4 tests): compone los predicados de `shared-utils` con el texto de error en español — capa de presentación, no de dominio.
    - **Hueco real detectado al montar la pantalla**: no existía ningún `QueryClientProvider` en el árbol — `useLogin()` habría fallado en tiempo real (los hooks de `src/api` solo se habían probado antes a nivel de función pura). Añadido a `app/_layout.tsx`.
    - **Diseño**: estética calcada del `NeuralLoader` (`BackgroundGrid`/`COLORS` reexportados) — no solo referencia visual, el propio `NeuralLoader` sustituye al formulario como estado de carga real mientras `useLogin` está pendiente. Error de credenciales incorrectas mostrado tal cual devuelve `LoginUseCase` (AC de US-002: "sin indicar cuál de los dos datos falló"), sin reinterpretarlo en cliente.
    - **Refactor posterior, misma tarea**: a petición del usuario, los `StyleSheet.create(...)` de `NeuralLoader.tsx`/`StatusPill.tsx`/`LoginScreen.tsx` se extrajeron a ficheros `*.styles.ts` sidecar (React Native no soporta imports de `.css` reales para iOS/Android — se preguntó explícitamente y se descartó esa vía, sin romper Metro/NodeNext).

    Verificado con bundle real de web (`expo export --platform web`, montado temporalmente en `app/index.tsx`) antes y después del refactor de estilos. `npx turbo run typecheck lint test` → 32/32 en verde (21/21 tests en `mobile-app`, 9/9 en `shared-utils`, nuevo paquete con código real).

41. ~~Hueco de proceso (recurrente): Security no había revisado LoginScreen/shared-utils~~ ✅ El usuario detectó que la tarea #40 no tenía entrada de Security pese a tocar validación de credenciales — mismo hueco ya señalado una vez antes (#28). Revisión real contra el checklist de la skill (OWASP Top 10, secretos, validación de inputs, dependencias, cumplimiento ADR-012), no un registro de trámite:
    - Confirmado sin regresión en el mensaje genérico de login (ADR-012 §4/US-002) y sin superficie de inyección nueva (nada derivado de input crudo se renderiza de vuelta).
    - Sin secretos ni dependencias de terceros nuevas (`MIN_PASSWORD_LENGTH` es política pública, `shared-utils` es interno al monorepo).
    - **Riesgo real detectado, no introducido por #40 pero relevante por primera vez**: `EXPO_PUBLIC_API_BASE_URL` por defecto es `http://` — es la primera vez que una contraseña real viaja por `fetchClient`; sin HTTPS en producción viajaría en texto claro por la red. Corregido con advertencia explícita en `.env.example`; aplicar HTTPS de verdad queda para DevOps Agent cuando exista despliegue real.
    - `isValidEmail` acepta dominios con puntos consecutivos (`a@b..com`) — limitación de UX documentada, no hallazgo de seguridad (el backend nunca ha validado formato de email).

    Sin cambios de código más allá del comentario en `.env.example`. Ningún hallazgo crítico.

42. ~~`LoginScreen`: refinamientos de diseño (caja centrada, fondo animado, enlace de registro)~~ ✅ El usuario probó la pantalla en el navegador (`npx expo start --web`) y pidió tres ajustes en la misma sesión de diseño: formulario dentro de una caja centrada, el fondo de partículas del `NeuralLoader` también en el login, un botón "Registrarse" arriba a la derecha, y un enlace de "recuperar contraseña" bajo el botón de enviar.
    - **Caja centrada + fondo de partículas**: nuevo estilo `card` (`LoginScreen.styles.ts`) envolviendo los campos, separado visualmente del fondo; `ParticleField` (antes solo interno a `NeuralLoader`) exportado desde `src/components` y añadido junto a `BackgroundGrid`.
    - **Hueco real detectado al arrancar el servidor de verdad por primera vez** (`expo start --web`, no solo `expo export`): con `experiments.typedRoutes` activo, `tsc` empezó a fallar en `router.replace('/(app)/home')` — la ruta no existe todavía y Expo Router genera un tipo estricto a partir de los ficheros reales de `app/`. No era un error introducido por esta tarea, solo no se había disparado hasta que el servidor generó los tipos. Corregido con `@ts-expect-error` (no `as any`): TS forzará a quitar la silenciación en cuanto `(app)/home` exista de verdad, en vez de dejar una `any` olvidada. `.gitignore`/`tsconfig.json#include` de `mobile-app` (generados por Expo CLI, `expo-env.d.ts`) añadidos al repo como configuración real, no artefactos temporales.
    - **Botón "Registrarse"**: hueco real — `react-native-safe-area-context` era dependencia desde el scaffolding inicial pero nunca se había montado ningún `SafeAreaProvider`; añadido a `app/_layout.tsx` (mismo patrón que el hueco de `QueryClientProvider` de #40) para poder posicionar el botón respetando el notch/status bar (`useSafeAreaInsets`). Enlaza a `/(auth)/register` con el mismo `@ts-expect-error` que `/(app)/home` — ruta prevista en ADR-015, todavía sin construir.
    - **Enlace de "recuperar contraseña"**: antes de implementarlo, se señaló que ni US-002 ni ADR-015 lo respaldan (ambos lo marcan explícitamente "fuera de alcance" / "no se inventan aquí") — a diferencia de Home/Registro, que sí están previstas. El usuario, consultado, eligió mostrarlo solo como texto atenuado sin acción (`Text`, no `TouchableOpacity`/`onPress`) — no inventar navegación real para un caso de uso que Product no ha definido.
    - Corregido de paso: `pointerEvents` como prop (deprecado en RN Web) → `style.pointerEvents` en `ParticleField`/`StatusPill`.

    Sin test automático (mismo criterio que el resto de UI visual de la sesión). `npx turbo run typecheck lint test` → 32/32 en verde, sin tests nuevos. Verificado de verdad en navegador (`http://localhost:8081/login`), no solo con `expo export`.

43. ~~`RegisterScreen` (US-001)~~ ✅ (TDD Red→Green) Segunda pantalla real, mismo patrón ya establecido por `LoginScreen`: fondo `BackgroundGrid`+`ParticleField`, formulario en `card` centrada, `*.styles.ts` sidecar, `useRegister` (ya implementado) reutilizado como `NeuralLoader` durante el envío.
    - **`validateRegisterForm`** (TDD, 5/5 tests): añade el campo `academicLevel` (obligatorio, US-001 "Nivel académico obligatorio") a los mismos predicados de `@mathmind/shared-utils` que ya usaba `LoginScreen.validation.ts`. `AcademicLevel` derivado de `RegisterRequestDto['academicLevel']` (`@mathmind/shared-types`, `import type`, se borra al compilar) en vez de añadir `@mathmind/shared-domain` como dependencia nueva solo por un alias de tipo.
    - **Selector rediseñado a 4 estrellas acumulativas** (a petición del usuario, mismo día): de 4 chips independientes a un rating de estrellas donde marcar la N-ésima marca también la 1..N-1 — "un ingeniero no puede ser sin tener primaria", el dato es ordinal, no una elección entre opciones sueltas. Glifo Unicode (`★`/`☆`), sin icon library nueva — mismo criterio que los símbolos matemáticos de `NeuralLoader` (`anatomyPaths.ts`) y que ya evitaba un `Picker` nativo. Una etiqueta bajo las estrellas informa el nivel en texto según la última estrella marcada.
    - **"Nivel académico" → "Nivel de complejidad"**: cambio de copy deliberado, a petición del usuario, para no asociar lingüísticamente "tu nivel personal" con "el nivel en el que quieres practicar" — mismo dato (`RegisterRequestDto.academicLevel`), ninguna URL/ruta/contrato tocado, la redacción exacta de US-001 ("indicando mi nivel académico") queda como está en el documento de historia, esto es solo copy de UI.
    - **Mensaje de error de servidor traducido, a diferencia de `LoginScreen`**: `RegisterUseCase` solo lanza dos mensajes reales, en inglés y con `exposeMessage: true` (`routes.ts`) — `"Email already registered: <email>"` y `"Password must be at least 8 characters"`. US-001 exige un texto concreto en español ("indicando que el email ya está en uso"), a diferencia de US-002, que solo exige que el mensaje sea genérico sin importar el idioma exacto — `describeRegisterError()` mapea el prefijo del mensaje real a español en vez de mostrarlo tal cual.
    - **Forcing function confirmada en la práctica**: el `@ts-expect-error` de `/(auth)/register` en `LoginScreen.tsx` (añadido en #42) pasó a ser un error real de TypeScript ("directiva no usada") en cuanto se creó `app/(auth)/register.tsx` — se quitó la silenciación, exactamente el comportamiento buscado al elegir `@ts-expect-error` sobre `as any`.
    - Enlace de vuelta a `(auth)/login` (ruta ya real, sin `@ts-expect-error`).
    - **Error transitorio observado en el log del servidor, no reproducible**: durante la reconstrucción de commits de la tarea anterior, el hot-reload de Metro capturó un instante intermedio con `NeuralLoader/index.ts` y `LoginScreen.tsx` desincronizados (uno sin exportar `ParticleField`, el otro ya importándolo) — autocorregido en el siguiente bundle; confirmado que el estado actual es consistente (`tsc`/`eslint`/`vitest` limpios, `curl` 200 en ambas rutas).

    5/5 tests nuevos (`RegisterScreen.validation.test.ts`). `npx turbo run typecheck lint test` → 32/32 en verde (26/26 tests en `mobile-app`, antes 21). Verificado en navegador real (`http://localhost:8081/register`).

44. ~~`EmailInput`/`PasswordInput`: componentes de input compartidos~~ ✅ El usuario pidió un `TextInput` personalizado reutilizable en dos variantes (email con validación de formato y, opcionalmente, "existencia" de usuario; contraseña con un "ojo" que revela el valor al mantener pulsado, en hover web o touch), y sustituirlos en `LoginScreen`/`RegisterScreen`. Motivación real, no solo la petición: `field`/`label`/`input`/`inputError`/`errorText` estaban duplicados byte a byte entre los `.styles.ts` de ambas pantallas.
    - **Mecanismo de "validación de existencia" aclarado antes de implementar** (AskUserQuestion): no una llamada de red nueva (sería, además, un vector de enumeración de cuentas — ADR-012) sino mostrar el error real que ya devuelve el servidor al enviar. `EmailInput`/`PasswordInput` quedan puramente presentacionales (`value`/`onChangeText`/`error`, sin lógica de validación ni red); en `RegisterScreen`, el error de "email ya registrado" se enruta específicamente al `error` de `EmailInput` en vez de solo al banner genérico — la realización concreta de lo pedido. `LoginScreen` mantiene el error de servidor solo en el banner genérico, sin tocar el campo de email (ADR-012: el login nunca debe indicar si un email existe).
    - **`PasswordInput`**: revelado por mantener pulsado (`Pressable` con `onHoverIn`/`onHoverOut` + `onPressIn`/`onPressOut`), no por alternar — lectura literal de "hover o touchscreen". Glifos Unicode (`👁️`/`🙈`), sin dependencia de iconos nueva.
    - **Estilos duplicados centralizados** en `src/components/inputs/styles.ts`; `LoginScreen.styles.ts` pierde `field`/`label`/`input`/`inputError`/`errorText` por completo, `RegisterScreen.styles.ts` conserva `field`/`label`/`errorText` (siguen usados por el selector de estrellas, local a esa pantalla).
    - **Caché de Metro obsoleta detectada al verificar, no un bug de código**: el servidor de desarrollo, corriendo desde tareas anteriores, fallaba de forma persistente (no transitoria, a diferencia del `ParticleField` de la tarea anterior) con `Unable to resolve module ./styles from EmailInput.tsx` pese a que el fichero existía con el contenido correcto en disco — caché de Metro desincronizada del proceso de larga duración. Corregido reiniciando con `expo start --web -c` (caché limpia); confirmado con un bundle completo sin errores (1329 módulos) antes de volver a modo watch normal.

    Sin test nuevo — presentacional puro, mismo criterio que el resto de UI visual de la sesión (26/26 tests de `mobile-app`, sin cambios de cantidad). `npx turbo run typecheck lint test` → **32/32 en verde** en todo el monorepo. Verificado con el bundle JS real (no solo la ruta HTML) y sin errores en el log del servidor, en ambas pantallas.
