export {}

// Barrel export pendiente. Environment configuration, validation schemas, feature flags
// -- ver ejemplos en ARCHITECTURE.md ("shared-config").
// Nota: no se eligio libreria de validacion (p. ej. zod) -- no estaba decidida en ADR-001,
// se deja pendiente para no introducir una dependencia de arquitectura sin documentar.
